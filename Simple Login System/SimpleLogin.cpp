#include <bits/stdc++.h>
#include <memory>

using namespace std;

int main() {
    std::shared_ptr<string> username = std::make_shared<string>();
    std::shared_ptr<string> password = std::make_shared<string>();
    string UsernameInput;
    while (true) {
        std::cout << "Enter Your Username: ";
    }
    return 0;
}